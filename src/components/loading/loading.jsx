import React from 'react';

import style from './loading.module.css';

const Loading = () => {
  return (
		<main className={style.main}>
			<h1 className={style.loading}>soon</h1>
		</main>
  );
}

export default Loading;