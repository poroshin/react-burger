import React from 'react';

export const DataIngredientsContext = React.createContext(null);
export const OrderDetailContext = React.createContext(null);
export const SelectedIngredientsContext = React.createContext(null);